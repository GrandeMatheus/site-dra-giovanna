---
import BaseLayout from "../layouts/BaseLayout.astro";
---

<BaseLayout>
  <section
    class="min-h-screen flex flex-col items-center justify-center text-center px-6 py-24 animate-fade-in"
  >
    <div
      class="bg-white/60 backdrop-blur-lg border border-brand-mint p-10 rounded-3xl shadow-xl max-w-xl w-full"
    >
      <h1 class="text-4xl font-bold text-brand-moss mb-4">
        Mensagem recebida com carinho! 💌
      </h1>
      <p class="text-lead mb-6">
        Obrigado por entrar em contato com a Dra. Giovanna. Sua mensagem foi
        enviada com sucesso e será respondida em breve com toda a atenção e
        cuidado que você merece.
      </p>
      <a
        href="/"
        class="inline-block bg-brand-mint text-white px-6 py-3 rounded-full shadow hover:bg-brand-pink hover:scale-105 transition font-medium"
      >
        Voltar para a página inicial
      </a>
    </div>
  </section>

  <style>
    @keyframes fade-in {
      from {
        opacity: 0;
        transform: translateY(20px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .animate-fade-in {
      animation: fade-in 0.8s ease-out;
    }
  </style>
</BaseLayout>
